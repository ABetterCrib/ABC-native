import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const RockerControl = (props) => {
    return (
        <Text>RockerControl</Text>
    )
}

const styles = StyleSheet.create({

});

export default RockerControl;